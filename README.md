# EcoBuddy

  This is a code bundle for EcoBuddy. The original project is available at https://www.figma.com/design/91W6q52l3HugCztqjjjllt/ECOHUB-Sustainability-Tracker.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.